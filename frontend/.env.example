# Keep this empty for local dev when using Vite proxy.
# For static production builds without same-origin rewrites, set this to your backend base URL.
VITE_API_BASE_URL=

# Local dev proxy target used by vite.config.ts
VITE_PROXY_API_TARGET=http://127.0.0.1:8766
